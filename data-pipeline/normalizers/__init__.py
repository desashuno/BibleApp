"""
Normalizer modules for transforming raw open-source Bible data into SQLite.

Each module exposes a `normalize(raw_dir, db)` function that reads from
raw/ and writes into the shared SQLite database.
"""
