package org.biblestudio

/**
 * Root composable entry point for BibleStudio.
 * Currently a placeholder — the full app shell is built in Phase 1.7.
 */
object AppInfo {
    const val NAME = "BibleStudio"
    const val VERSION = "0.1.0"
    const val DATABASE_VERSION = 39
}
